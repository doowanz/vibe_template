# CLAUDE.md

## 🌟 스터디 목표
- "생각한 것을 바로 코드로!" (Vibe Coding)
- 복잡한 이론보다 '동작하는 서비스'를 우선합니다.
- 비개발자 팀원도 이해할 수 있는 쉬운 코드를 지향합니다.

## 🛠 기술 스택
- 프레임워크: Next.js (App Router)
- 스타일: Tailwind CSS + shadcn/ui
- 데이터베이스: Supabase
- 배포: Vercel (GitHub 연동 자동 배포)

## ✍️ 코딩 스타일 (Vibe Rules)
1. **쉬운 주석:** 모든 함수와 복잡한 로직 위에는 한글로 "이 코드가 무엇을 하는지" 초등학생도 이해할 수 있게 설명해 주세요.
2. **간결한 구조:** 파일을 너무 쪼개지 마세요. 한 파일 안에서 프론트와 백엔드 로직(Server Actions)을 처리하는 것을 권장합니다.
3. **디자인 우선:** 비개발자에게 시각적 만족감은 중요합니다. shadcn/ui를 적극 활용해 현대적이고 깔끔한 UI를 먼저 만들어주세요.
4. **에러 가이드:** 에러가 발생하면 해결책만 주지 말고, "왜 이런 에러가 났는지"를 스터디원에게 과외 선생님처럼 친절하게 설명해 주세요.

## 📁 폴더 규칙 (최대한 단순하게)
- `app/`: 페이지와 기능 (Next.js 규칙)
- `components/`: 재사용할 버튼이나 입력창
- `lib/`: Supabase 설정 등 공통 도구
*그 외 폴더는 Claude가 판단해서 최소한으로 생성해 줘.*

## 🔐 보안 및 환경 변수
1. **`.env.local` 보호:** 민감한 키가 들어있습니다. 절대로 GitHub에 올리지 마세요.
2. **`.gitignore` 확인:** `.env.local`이 반드시 포함되어 있는지 확인하세요.
3. **환경 변수 가이드:** 새로운 환경 변수가 필요하면 `.env.local`에 추가할 값을 명확히 안내하고, `.env.example`도 함께 업데이트하세요.
4. **키 노출 금지:** `SUPABASE_SERVICE_ROLE_KEY` 같은 관리자 키는 절대 브라우저(Client Side) 코드에서 사용하지 마세요.
5. **Vercel 환경 변수:** `.env.local`의 값은 Vercel 대시보드 → Settings → Environment Variables에도 동일하게 등록해야 배포 환경에서 정상 동작합니다.

## 🌿 Git & Branch Rules
- 기본 브랜치: `main`
- 모든 배포는 `main` 브랜치 기준으로 진행합니다.
- Vercel은 `main` 브랜치에 push할 때마다 자동으로 배포됩니다.

## 🚀 배포 환경
- 플랫폼: Vercel
- 배포 방식: GitHub `main` 브랜치 push → 자동 배포
- 배포 설정 참조: `DEPLOY.md`

## 💬 Claude에게 보내는 특별 지시
- 코드를 짜기 전에 "어떤 방식으로 구현할지" 먼저 한국어로 제안하고 승인을 받으세요.
- 어려운 기술 용어는 반드시 쉬운 말로 풀어서 설명해 주세요.
- 새로운 라이브러리나 도구가 더 편리하다면 적극적으로 제안해 주세요.

## Automation Rules
- 패키지 설치나 환경 설정 시 최선의 선택지를 고르고 즉시 진행하세요.
- Vercel 배포에 영향을 주는 설정 변경 시 반드시 사용자에게 확인 후 진행하세요.
